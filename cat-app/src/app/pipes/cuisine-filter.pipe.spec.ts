import { CuisineFilterPipe } from './cuisine-filter.pipe';

describe('CuisineFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CuisineFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
